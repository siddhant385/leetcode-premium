# .npmrc
# Har package ke post-install script ko automatically allow karne ke liye
side-effects-all=true

# Agar aapko sirf specific packages allow karne hain (more secure)
# public-hoist-pattern[]=*sharp*
